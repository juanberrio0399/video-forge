node_modules/
renders/
_artifact*/
_cmp_*.mp3
.debug/
.cache/
*.log
.DS_Store
Thumbs.db
.env
.env.*

# Voz de referencia de Juan = PERSONAL. NUNCA en el repo (menos si es publico).
# Vive en R2 (privado); los workflows la bajan de ahi. Ver README / voice_parallel.yml.
assets/voice/*.mp3
assets/voice/*.wav

# Salidas de prueba locales (nunca van al repo)
_*/
_*.mp3
_*.mp4

.wrangler/
